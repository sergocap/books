h1 Редактирование книги "{{old_title}}"
table.table
  thead
    tr
      td Название
      td Описание
      td Обложка
      td.date_col Дата создания
      td

  tbody
    tr.form-group
      td
        textarea.form-control[bindon-ngModel='book.title']

      td
        textarea.form-control[cols='60' rows='10' bindon-ngModel='book.description']

      td
        a[target='blank' *ngIf='!poster_base64.length' bind-href='book.poster_original_url']
          img[bind-src='book.poster_url']

        img[bind-src='poster_base64']

        input#book_poster.form-control[type='file' name='book[poster]'
          on-change='set_poster_base64($event)' bindon-ngModel='files_input']

      td {{book.created_at | amLocale: 'ru' | amDateFormat: 'LL'}}

      td
        a.btn.btn-success[href='javascript:void(0)' on-click='update(book)'] Сохранить
        a<[data-confirm='Вы уверены?' data-method='delete' bind-href='"/books/" + book.id'] Удалить



